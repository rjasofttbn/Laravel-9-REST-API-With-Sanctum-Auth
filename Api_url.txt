After login:
Get into the Postman app’s Authorization tab, select ‘Bearer Token’ from the 
Type dropdown, then add the auth token that you got when logged into the app.

http://localhost:8000/api/blogs (get)

http://localhost:8000/api/blogs/{id}
http://localhost:8000/api/blogs (get)

http://localhost:8000/api/blogs (post)

http://localhost:8000/api/blogs/{id} (put)

http://localhost:8000/api/blogs/{id} (delete)
